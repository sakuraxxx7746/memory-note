services:
  app:
    build: .
    ports:
      - '3000:3000'
    volumes:
      # ソースコードをバインドマウント
      - .:/usr/src/app
    environment:
      - NODE_ENV=development
    env_file:
      - .env.local
    stdin_open: true
    tty: true

volumes:
  node_modules:
